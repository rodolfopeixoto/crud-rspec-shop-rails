

<h1>List Clients</h1>

<table>
  <thead>
    <tr>
      <td>
        Nome
      </td>
      <td></td>
      <td></td>
    </tr>
  </thead>
  <tbody>
    <% @customers.each do |customer| %>
      <tr>
        <td>
          <%= customer.name %>
        </td>
        <td><%= link_to 'Show', customer_path(customer) %></td>
        <td><%= link_to 'Edit', edit_customer_path(customer) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'New Client', new_customer_path %>